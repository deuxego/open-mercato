import type { ModuleInfo } from '@open-mercato/shared/modules/registry'

export const metadata: ModuleInfo = {
  name: 'inngest',
  title: 'Inngest',
  description: 'Inngest workflow orchestration for durable multi-step workflows',
  version: '0.1.0',
}
